[pytest]
# 개발용 테스트 설정 — 운영 봇(run_all.py)과 무관, dev 전용.
# 실행: python -m pytest   (의존성: pip install -r requirements-dev.txt)
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
